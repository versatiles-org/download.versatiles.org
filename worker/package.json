{
  "name": "download-versatiles-org-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker fronting the download.versatiles.org R2 bucket (index routing + range/CORS).",
  "scripts": {
    "check": "npm run typecheck",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.103.0"
  }
}
