# Supply-chain hardening: do not run dependency lifecycle install scripts.
# The Workers toolchain (esbuild, sharp, workerd) ships platform binaries as
# separate packages, so they work without their install scripts.
ignore-scripts=true
